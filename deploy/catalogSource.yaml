apiVersion: operators.coreos.com/v1
kind: OperatorSource
metadata:
  name: nuodb-operator
  namespace: openshift-marketplace
spec:
  type: appregistry
  endpoint: https://quay.io/cnr
  registryNamespace: nuodb
  displayName: "NuoDB Operator"
  publisher: "NuoDB Inc."
